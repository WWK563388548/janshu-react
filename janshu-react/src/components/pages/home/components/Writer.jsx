import React, { Component } from 'react';
import {
    WriterWrapper,
} from '../home_style';

class Writer extends Component {
    render() {
        return (
            <WriterWrapper>
                Writer
            </WriterWrapper>
        );
    }
}

export default Writer;